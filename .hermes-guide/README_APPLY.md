# 文件说明

把这两个文件放进 HanabePhoto 项目：

1. `docs/HERMES_MASTER_GUIDE.md`
2. 将 `AGENTS_HERMES_INSERT.md` 的内容合并到项目现有 `AGENTS.md` 靠前位置

注意：

- 不建议直接用 `AGENTS_HERMES_INSERT.md` 覆盖现有 `AGENTS.md`。
- 当前 ChatGPT 环境没有访问到你的 GitHub 仓库，也不能直接写入你 Windows 本地 `D:\APP`，所以这里生成的是可直接落项目的文件。
- Hermes 之后应以项目当前仓库为事实来源；这份总纲本身也明确要求在规则与未来当前代码冲突时以当前代码/文档/运行结果为准。
