# 下一任务交接：Hanabe 悬浮窗任务状态闭环

> 接手关键词：`HANABE-FLOATING-STATUS-V2`
>
> 用户要求：不要再次询问方案选择，直接依照本文实现；音效暂缓，不在本任务修复。

## 1. 接手前必读

依次读取 `docs/HERMES_MASTER_GUIDE.md`、`AGENT_HANDOFF.md`、`docs/current-status.md`、`docs/known-issues.md`、`docs/design-system.md` 和本文件。

项目固定在 `D:\HanabePhoto`，不要在 OneDrive 创建项目、工作树或发布目录。当前分支为 `master`；开始前检查 `git status`，保留用户未跟踪文件，不要擅自加入安装器素材。

## 2. 当前真实状态

- 悬浮窗位于 `src/HanabePhotoManager.App/HanabeAssistantWindow.xaml(.cs)`。
- `HanabeAssistantWindowPolicy.ShouldShow` 规定：仅主窗口最小化且用户启用助手时显示；主窗口恢复后隐藏。此行为正确，必须保留。
- 已支持拖动、隐藏当次窗口、无虚线；已有 Q 版动态、8-bit 像素动态、静态、关闭四种样式。
- 当前状态只有 `Idle / Scanning / Checking / Importing / Completed / Error`。
- 当前只显示角色、`ProgressLabel`、进度条、百分比和预计剩余时间。
- 严重语义缺口：主动取消、等待安全停止、真正异常、恢复失败都可能映射成 `Error`；例如 `CancelCurrentTask()` 直接调用 `SetAssistantState(Error)`。
- 导入已有磁盘恢复点和断点续传；已完成文件保留，未完成项可继续，但悬浮窗没有清楚表达。
- 重复检测完成/清理完成已有独立报告与“是否恢复主窗口”设置，不能回退。
- 声音设置已有 UI 和实现，但用户机器仍无声；用户明确要求暂缓，下一任务不要调音频，也不要宣称已修复。
- 当前远端基线提交应为 `90d0f50 fix: play Hanabe sounds through native WAV output`，接手时重新核对本地与 `origin/master`。

## 3. 目标：让后台任务状态可理解、可恢复、可操作

用户最关心：现在做什么、做到哪里、是否还在传输、为什么停止、是否安全、能否恢复、下一步做什么。

### 3.1 状态模型

至少覆盖下列用户可感知状态，命名可随架构调整，但不能继续全部塞进 `Error`：

| 状态 | 主文案示例 | 必须说明 |
|---|---|---|
| 空闲 | Hanabe 正在待命 | 不伪造进度 |
| 扫描 | 正在扫描媒体 | 已发现数量、当前阶段 |
| 校验 | 正在校验重复内容 | 文件名校验 → SHA-256 阶段 |
| 导入 | 正在安全传输 | 当前/总数、速度、剩余时间 |
| 请求停止 | 正在安全停止… | 当前文件安全点停止，不等于失败 |
| 已取消 | 任务已停止 | 已完成内容保留、未完成内容处理方式 |
| 意外中断 | 任务意外中断 | 简短原因、是否有恢复点 |
| 等待恢复 | 可继续上次导入 | 剩余项数量、恢复入口 |
| 正在恢复 | 正在恢复上次导入 | 核对续传/继续传输阶段 |
| 完成 | 任务完成 | 成功、跳过、失败摘要 |
| 完成但有问题 | 已完成，但有项目需处理 | 失败/冲突数量、报告入口 |
| 不可恢复错误 | 任务失败 | 人类可读原因、返回主窗口入口 |

不要伪造“暂停”：只有底层真正支持暂停/继续时才加入 `Paused`。当前已有取消与断点恢复，不等于暂停。

### 3.2 信息层级

保持紧凑，建议宽 380–420px、高度按状态自适应：

1. 角色名 `Hanabe` + 状态徽标 + 隐藏按钮。
2. 任务名，如“导入 08.27 相机文件”。
3. 阶段详情，如“正在传输 DSC01234.ARW · 286 / 1058”；只显示 basename，不泄露完整路径。
4. 确定进度显示百分比；总量未知用不确定进度，不能显示假的 0%。
5. 按可用性显示速度、剩余时间、成功/跳过/失败；无数据时隐藏，不堆 `--`。
6. 终态明确说明“已完成文件已保留”“可稍后继续”等结果。
7. 只显示当前可执行的操作。

### 3.3 操作规则

- 运行中：`停止任务` + `打开 Hanabe`。
- 停止请求已发出：禁用重复停止，文案 `正在停止…`。
- 已取消且可恢复：`稍后继续` + `打开 Hanabe`；不要自动恢复。
- 异常且有恢复点：`查看原因` + `打开并恢复`。
- 完成/完成但有问题：`查看报告` + `知道了`。
- 点击角色或任务主体可恢复主窗口并定位对应页面；不要主动抢焦点。
- 保留拖动，拖动区域不能吞按钮点击。
- “隐藏本次悬浮窗”在本次最小化周期内保持隐藏；主窗口恢复后再次最小化可重新出现。

### 3.4 动画映射

Q 版与像素动态使用相同语义：工作阶段用对应工作动画；请求停止用收尾/等待动画；已取消用平静停止动画；异常用错误动画；完成用庆祝动画。资源缺失必须回退静态角色，不能空白。本任务不重新生成角色素材，优先复用 `Assets/Hanabe`。

## 4. 实现边界

- 不要让 XAML 拼接业务文案。新增面向悬浮窗的结构化展示快照/ViewModel，统一提供标题、详情、统计、动作可见性和可访问名称。
- `ProgressLabel` 与 `StatusMessage` 仍服务主界面，不宜继续无限复用。
- 生命周期至少保证：

```text
Running → StopRequested → Canceled
Running → Interrupted → Recoverable
Recoverable → Resuming → Completed / CompletedWithIssues / Interrupted
Running → Completed / CompletedWithIssues / Failed
```

- 用户取消产生 `Canceled`；非取消异常才产生 `Interrupted` 或 `Failed`。
- 不再把所有终态固定 3 秒后无条件切回 Idle。完成可短暂收起；错误、可恢复中断、完成但有问题应保留到用户查看或关闭。
- 导入速度使用滑动窗口统计；只有实际字节传输任务显示速度。
- 停止仍只在当前文件安全点生效；不能破坏原子写入与临时文件清理。

## 5. 验收场景

1. 主窗口正常/最大化时不可见；最小化时可见；恢复后立即隐藏。
2. 可拖动、无虚线、按钮点击不触发拖动。
3. 扫描总量未知时为不确定进度。
4. 正常导入显示当前项、计数、速度、预计剩余时间。
5. 导入中停止：先“正在安全停止”，实际结束后“已停止”，绝不显示“错误”。
6. 模拟异常：显示“意外中断”，准确说明有无恢复点。
7. 重启发现恢复点：显示“可继续上次导入”；恢复后转“正在恢复”。
8. 完成含失败项：显示“完成但有问题”，可打开报告。
9. 正常完成显示成功/跳过/失败摘要。
10. Q 版、像素、静态、关闭分别验证；资源缺失验证静态回退。
11. 重复检测/清理报告与自动恢复主窗口设置不回退。

## 6. 测试与交付

- 为状态转换、显示策略、按钮可见性、终态停留策略增加单元测试。
- 按 `docs/testing.md` 构建、运行相关测试并在完成前运行完整测试。
- 更新 `docs/current-status.md`、`docs/known-issues.md`（如适用）、`docs/agent-change-log.md`。
- 音效继续标记为暂缓，不处理。
- 不提交现有未跟踪安装器素材、运行数据或个人路径。
- 完成后提交并推送 GitHub，提交信息聚焦悬浮窗状态闭环。

## 7. 新任务首条消息（可直接复制）

> `HANABE-FLOATING-STATUS-V2`。请在 `D:\HanabePhoto` 读取 `AGENTS.md`、`docs/HERMES_MASTER_GUIDE.md`、`AGENT_HANDOFF.md` 和 `AGENT_HANDOFF_NEXT.md`，直接实现“Hanabe 悬浮窗任务状态闭环”。重点修复用户主动停止被误判为 Error，完整覆盖扫描、校验、导入、安全停止、已取消、意外中断、可恢复、恢复中、完成、完成但有问题和失败；显示当前文件、当前/总数、速度、剩余时间及结果摘要，并提供与状态匹配的停止、打开、恢复和查看报告操作。保留仅最小化显示、可拖动、无虚线、Q版/像素/GIF 回退及重复检测报告行为。音效暂缓，不要处理。无需向我确认方案，按交接文档实现、测试、提交并推送。
