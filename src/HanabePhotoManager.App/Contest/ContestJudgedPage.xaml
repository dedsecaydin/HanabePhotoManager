<UserControl x:Class="HanabePhotoManager.App.Contest.ContestJudgedPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:HanabePhotoManager.App.Contest" xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
  <Grid Background="{DynamicResource Brush.Background.Canvas}"><Grid.ColumnDefinitions><ColumnDefinition Width="320"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <Border Style="{StaticResource Card.Default}"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><DockPanel Margin="0,0,0,16"><TextBlock Text="欣赏项目" Style="{StaticResource Layout.SectionTitle}"/><Button DockPanel.Dock="Right" Content="批量下载获奖作品" Style="{StaticResource Button.Secondary}" Click="DownloadAll_Click"/></DockPanel><ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"><ItemsControl Name="JudgedContestList"><ItemsControl.ItemTemplate><DataTemplate DataType="{x:Type local:ContestItem}"><Border Margin="0,0,0,8" Style="{StaticResource Card.Interactive}" Cursor="Hand" MouseLeftButtonDown="ContestCard_Click"><StackPanel><TextBlock FontWeight="SemiBold" TextWrapping="Wrap" Text="{Binding Name}"/><TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" TextWrapping="Wrap" FontSize="{StaticResource Typography.BodySmall}" Margin="0,6,0,0" Text="{Binding Description}"/><TextBlock Foreground="{DynamicResource Brush.Text.Tertiary}" FontSize="{StaticResource Typography.Caption}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" Text="{Binding Url}"/></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></ScrollViewer></Grid></Border>
    <Grid Grid.Column="2"><wv2:WebView2 Name="Browser"/><Border Name="EmptyState" Style="{StaticResource Status.Panel}" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="460"><StackPanel><TextBlock Text="选择一个欣赏项目查看获奖作品" Style="{StaticResource Status.Title}" TextAlignment="Center"/><TextBlock Text="左侧列出已评奖摄影赛事。选择任意一项即可查看官方获奖作品页面。" Style="{StaticResource Status.Description}" TextAlignment="Center"/></StackPanel></Border></Grid>
  </Grid>
</UserControl>
