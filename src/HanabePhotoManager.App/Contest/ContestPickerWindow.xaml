<Window x:Class="HanabePhotoManager.App.Contest.ContestPickerWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:HanabePhotoManager.App.Contest" Title="选择摄影大赛" Width="600" Height="500" Style="{StaticResource Dialog.Window}">
  <Border Margin="16" Style="{StaticResource Dialog.Surface}">
    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
      <TextBlock Text="选择要下载获奖作品的大赛" Style="{StaticResource Dialog.Title}" Margin="0,0,0,16" />
      <ListBox Name="ContestList" Grid.Row="1" SelectionMode="Single" Padding="0" Style="{StaticResource List.Default}" ItemContainerStyle="{StaticResource ListItem.Default}">
        <ListBox.ItemTemplate><DataTemplate DataType="{x:Type local:ContestItem}"><StackPanel><TextBlock FontWeight="SemiBold" Text="{Binding Name}"/><TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="{StaticResource Typography.Caption}" Margin="0,4,0,0" Text="{Binding Category}"/></StackPanel></DataTemplate></ListBox.ItemTemplate>
      </ListBox>
      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
        <Button Content="取消" Margin="0,0,8,0" Style="{StaticResource Button.Secondary}" Click="Cancel_Click"/>
        <Button Content="下载" Style="{StaticResource Button.Primary}" Click="Ok_Click"/>
      </StackPanel>
    </Grid>
  </Border>
</Window>
