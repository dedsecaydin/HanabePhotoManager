<Window x:Class="HanabePhotoManager.App.Duplicates.DuplicateCleanupReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="重复文件清理报告" Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Style="{DynamicResource Dialog.Window}">
  <Border Margin="16" Style="{DynamicResource Dialog.Surface}">
    <StackPanel>
      <TextBlock Text="重复文件清理完成" Style="{DynamicResource Dialog.Title}" />
      <TextBlock x:Name="SummaryText" Margin="0,10,0,0" Style="{DynamicResource Dialog.Body}" TextWrapping="Wrap" />
      <UniformGrid Columns="3" Margin="0,18,0,0">
        <StackPanel><TextBlock x:Name="DeletedText" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center"/><TextBlock Text="已清理" HorizontalAlignment="Center" Opacity="0.7"/></StackPanel>
        <StackPanel><TextBlock x:Name="SkippedText" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center"/><TextBlock Text="跳过" HorizontalAlignment="Center" Opacity="0.7"/></StackPanel>
        <StackPanel><TextBlock x:Name="FailedText" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center"/><TextBlock Text="失败" HorizontalAlignment="Center" Opacity="0.7"/></StackPanel>
      </UniformGrid>
      <TextBlock x:Name="FreedText" Margin="0,18,0,0" FontWeight="SemiBold" HorizontalAlignment="Center" />
      <Button Content="知道了" IsDefault="True" Width="100" Margin="0,20,0,0" HorizontalAlignment="Right" Style="{DynamicResource Button.Primary}" Click="Close_Click" />
    </StackPanel>
  </Border>
</Window>
