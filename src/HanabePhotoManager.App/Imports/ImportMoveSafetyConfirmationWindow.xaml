<Window x:Class="HanabePhotoManager.App.Imports.ImportMoveSafetyConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hanabe 安全确认" Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Style="{DynamicResource Dialog.Window}" Loaded="Window_Loaded">
  <Border Margin="16" Style="{DynamicResource Dialog.Surface}">
    <Grid>
      <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="52" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
        <Border Width="40" Height="40" CornerRadius="20" Background="{DynamicResource Brush.PrimaryContainer}" VerticalAlignment="Top">
          <TextBlock Text="!" FontSize="{StaticResource Typography.Title}" FontWeight="Bold" Foreground="{DynamicResource Brush.OnPrimaryContainer}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <StackPanel Grid.Column="1">
          <TextBlock Text="确认以移动模式导入？" Style="{DynamicResource Dialog.Title}" />
          <TextBlock Text="这是会删除来源文件的操作，请在继续前确认来源设备和目标日期文件夹。" Style="{DynamicResource Dialog.Body}" Margin="0,6,0,0" />
        </StackPanel>
      </Grid>
      <Border Grid.Row="1" Margin="0,18,0,0" Padding="14" CornerRadius="{StaticResource Radius.Control}" Background="{DynamicResource Brush.Surface.ContainerLow}" BorderBrush="{DynamicResource Brush.OutlineVariant}" BorderThickness="1">
        <StackPanel>
          <TextBlock Text="Hanabe 只会在以下步骤全部成功后删除来源：" FontWeight="SemiBold" />
          <TextBlock Text="1. 文件完整复制到最终日期文件夹" Style="{DynamicResource Dialog.Body}" Margin="0,8,0,0" />
          <TextBlock Text="2. SHA-256 校验确认目标内容一致" Style="{DynamicResource Dialog.Body}" Margin="0,4,0,0" />
          <TextBlock Text="3. 原始文件名备注写入并验证完成" Style="{DynamicResource Dialog.Body}" Margin="0,4,0,0" />
        </StackPanel>
      </Border>
      <TextBlock Grid.Row="2" Text="如果不确定，请返回检查；取消不会复制、移动或删除任何媒体。" Style="{DynamicResource Dialog.Body}" Margin="0,14,0,0" />
      <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
        <Button x:Name="BackButton" Content="返回检查" IsCancel="True" Width="112" Style="{DynamicResource Button.Secondary}" Click="Back_Click" Margin="0,0,10,0" />
        <Button Content="确认移动并导入" Width="156" Style="{DynamicResource Button.Primary}" Click="Confirm_Click" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
